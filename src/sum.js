/**
 * sum two finite numbers
 *
 * @param {number} a - finite number
 * @param {number} b - finite number
 * @return {number} - sum of a and b
 *
 * @example
 *    sum(9, 16) => 25
 */

function sum(a, b) {
  return a + b;
}

export { sum };
